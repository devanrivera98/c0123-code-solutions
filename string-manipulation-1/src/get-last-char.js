/* exported getLastChar */
// define a function getLastChar that accepts a string as an arguement
// find the string length and subtract it by one to find the index of the string length
// return the string once you find the index of the last character
function getLastChar(string) {
  return string[string.length - 1];
}
