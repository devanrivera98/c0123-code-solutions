/* exported getFirstChar */

function getFirstChar(string) {
  return string[0];
}

// define a function named getFirstChar that accepts one argument of string
// grab the index of the first letter in the string & return that value
